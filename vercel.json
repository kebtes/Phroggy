{
    "functions": {
        "api/webhook.py":{
            "runtime": "python 3.10"
        }
    },
    "routes": [
        {
            "src": "/api/webhook",
            "dest": "/api/webhook.py"
        }
    ]
}